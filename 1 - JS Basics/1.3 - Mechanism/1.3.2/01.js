// Crea una función que muestre los números del 1 al 100

const showNumbers = () => {
  // 👇🏻 Añade tu lógica aquí
  let numbers = 0;

  while (numbers < 100) {
    numbers += 1;
    console.log(numbers);
  }

  // 👆🏻
};

showNumbers();
